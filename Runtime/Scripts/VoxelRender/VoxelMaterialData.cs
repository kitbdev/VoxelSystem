
namespace VoxelSystem {
    public struct VoxelMaterialData {
        // holds a reference to the voxel mat or per instance data
        public int idInCollection;
        // per voxel color or other data?
    }
}