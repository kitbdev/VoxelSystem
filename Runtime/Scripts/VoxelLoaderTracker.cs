using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace VoxelSystem {
    /// <summary>
    /// Trackers tell world to load when they move around. 
    /// VoxelWorld loads high res data around trackers. 
    /// Auto added to main camera if none found.
    /// </summary>
    public class VoxelLoaderTracker : MonoBehaviour {
        // todo
        // todo in voxworld, if none found auto add to main cam
    }
}